/*
* //class of js
* class ""{
*   cunstructor() // 생성자
*   get something() // getter
*   set something() // setter
*   static function() // can't call use for instance
*   extends // inherit - if you want to use this.val you have to use super() before inherit
*
* }
*
* */


/*
- patterns -
singleton pattern - 각 클래스당 한 개의 객체만 유지
iterator pattern - 효율적으로 객체 컬렉션을 반복할 수 있게 해줌. next메소드를 사용해서 다음 순서의 요소를 반환
decorator pattern - 상황에 따라 객체에 부가기능을 추가

*/
